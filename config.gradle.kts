extra.apply {
    // Config
    set("applicationId", "com.minaMikhail.networkCaching")
    set("compileSdk", 34)
    set("minSdk", 24)
    set("versionCode", 1)
    set("versionName", "1.0.0")
    set("jvmVersion", "17")
    set("testRunner", "androidx.test.runner.AndroidJUnitRunner")
    set("proguardFileName", "proguard-rules.pro")
    set("defaultProguardFileName", "proguard-android.txt")

    // Build Types
    set("debugBuildType", "debug")
}